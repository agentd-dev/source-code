# Keep the build context small and the build hermetic: the builder
# compiles from source, so ship sources only.
target/
.git/
# The agentd image build (cargo build -p agentd) does not need the web app.
web/
**/node_modules/
dist/
*.tar.gz
*.zip
**/*.log
