# Rust
/target
**/*.rs.bk

# Editors
.idea/
.vscode/
*.swp

# OS noise
.DS_Store

# Bench runner output (generated scorecards / ablation reports)
/bench/scorecard.json
/bench/ablation.json
__pycache__/

# Copied in at site-build time from the repo-root installer (single source of truth).
web/public/install.sh
# The A2A spec oracle is its own workspace, so its target dir is its own too.
crates/a2a-oracle/target/
