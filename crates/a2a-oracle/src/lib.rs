// SPDX-License-Identifier: AGPL-3.0-only
//! Nothing lives here. The crate exists to host `tests/wire.rs`, which checks
//! agentd's A2A wire against an independent implementation of the same
//! specification. See this crate's `Cargo.toml` for why it is a separate,
//! non-default workspace member.
