{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "caller intent",
  "type": "object",
  "required": ["intent", "reply"],
  "additionalProperties": false,
  "properties": {
    "intent": { "type": "string", "enum": ["sales", "support", "operator"] },
    "reply": { "type": "string", "minLength": 1, "maxLength": 600 }
  }
}
